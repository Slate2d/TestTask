# CRM System (FastAPI + React)

Тестовое задание: Полнофункциональная CRM система с разделением прав доступа (Admin/User).
Проект реализует монорепозиторий, содержащий Backend (Python) и Frontend (React), запускаемые через Docker Compose.

## 🛠 Технологический стек

* **Backend:** Python 3.11, FastAPI, SQLModel (SQLAlchemy + Pydantic), PostgreSQL, Alembic, Pytest.
* **Frontend:** React.js, CSS Modules, Axios, React Router, Nginx.
* **Инфраструктура:** Docker, Docker Compose.

## ✨ Функционал

Веб-интерфейс состоит из 5 вкладок (защищены авторизацией):

1.  **Создание клиента:** Форма добавления клиента (ФИО, телефон, пол). Автоматическое заполнение полей автора и статуса.
2.  **Список клиентов:** Таблица с поиском, пагинацией (infinite scroll ready).
    * *Админ:* Может удалять клиентов и менять статус (Active/Disabled).
    * *Пользователь:* Только просмотр и редактирование данных.
3.  **Добавление товара:** Форма привязки товара к клиенту с "живым" поиском клиента по ФИО.
4.  **Фильтр товаров:** Фильтрация по статусу, названию и ID клиента.
    * *Админ:* Удаление товаров, редактирование (модальное окно).
    * *Пользователь:* Только просмотр и редактирование.
5.  **Логи аудита (Только Админ):** История всех действий (создание, изменение, удаление) с записью "До" и "После".

**Особенности:**
* **RBAC (Role Based Access Control):** Разделение прав на Администратора и Пользователя.
* **JWT Auth:** Безопасная аутентификация.
* **Адаптивный дизайн:** Корректное отображение на мобильных устройствах.

## 🚀 Запуск проекта

Для запуска требуется установленный **Docker** и **Docker Compose**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/your-username/crm-project.git](https://github.com/your-username/crm-project.git)
    cd crm-project
    ```

2.  **Создайте файл `.env`** в папке `crm_backend/`:
    ```ini
    POSTGRES_SERVER=db
    POSTGRES_USER=crm_user
    POSTGRES_PASSWORD=strongpassword
    POSTGRES_DB=crm_db
    DATABASE_URL=postgresql+asyncpg://crm_user:strongpassword@db:5432/crm_db
    SYNC_DATABASE_URL=postgresql+psycopg2://crm_user:strongpassword@db:5432/crm_db
    SECRET_KEY=change_this_to_a_secure_random_string
    ALGORITHM=HS256
    ACCESS_TOKEN_EXPIRE_MINUTES=60
    ```

3.  **Запустите проект:**
    Перейдите в папку бэкенда (где лежит `docker-compose.yml`) и выполните:
    ```bash
    cd crm_backend
    docker-compose up --build
    ```

При первом запуске скрипт автоматически применит миграции БД и создаст тестовых пользователей.

## 🔑 Доступы

После запуска сервис доступен по адресам:

* **Frontend:** [http://localhost:3000](http://localhost:3000)
* **Backend Docs (Swagger):** [http://localhost:8000/docs](http://localhost:8000/docs)

**Тестовые учетные записи:**

| Роль | Логин | Пароль | Доступ |
| :--- | :--- | :--- | :--- |
| **Администратор** | `admin` | `adminpass` | Полный доступ, Логи аудита, Удаление |
| **Пользователь** | `user` | `userpass` | Вкладки 1-4, без удаления |

## 📂 Структура проекта

```text
.
├── crm_backend/          # FastAPI приложение
│   ├── alembic/          # Миграции БД
│   ├── app/
│   │   ├── api/          # Роутеры и контроллеры
│   │   ├── core/         # Конфиги и безопасность
│   │   ├── db/           # Подключение к БД
│   │   └── models/       # SQLModel модели
│   ├── Dockerfile
│   ├── docker-compose.yml
│   └── start.sh          # Скрипт запуска и миграций
│
└── crm_frontend/         # React приложение
    ├── src/
    │   ├── api/          # Настройка Axios
    │   ├── components/   # Переиспользуемые компоненты
    │   ├── context/      # Auth Context
    │   ├── hooks/        # Кастомные хуки
    │   └── pages/        # Страницы с CSS Modules
    ├── Dockerfile        # Multi-stage сборка (Node -> Nginx)
    └── nginx.conf        # Конфиг для React Router